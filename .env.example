# Credentials for the acquisition pipeline. Copy this file to `.env` in the project root
# and fill in your own keys. `.env` is gitignored and must never be committed.

FMP_API_KEY=
FINNHUB_API_KEY=

# Optional — override the location of the environment file
# DATA1002_ENV_FILE=/path/to/.env
